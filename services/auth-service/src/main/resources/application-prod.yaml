server:
  shutdown: graceful
spring:
  lifecycle:
    timeout-per-shutdown-phase: 30s
jwt:
  secret: ${JWT_SECRET}
  expiration-ms: ${JWT_EXPIRATION_MS}
auth:
  allowed-client-ids: ${ALLOWED_CLIENT_IDS}
  admin-client-ids: ${ADMIN_CLIENT_IDS}
